#terse-b: Bunyan lib that is terse to write.

I noticed my log files were getting large. Also, I need loggin in each file. So I need something terse.

If node env is 'PROD' it will not log INFO level or below.

Takes argument for class to get name.

